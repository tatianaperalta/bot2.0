def invite(welcome_message):
    invite = welcome_message.lower()

    return 'Hola' + names,
           #+ names + ',\n'

    #pg.typewrite(invite == 'Hola' + names + ',\n')
    #pg.press('enter')
    #time.sleep(10)
    #pg.hotkey('ctrl', 'w')

    # print('You say: ', bot_invite)
    # pt.typewrite(bot_invite, interval=.1)
    # pt.typewrite('\n')  # Sends the message (Disable it while testing)